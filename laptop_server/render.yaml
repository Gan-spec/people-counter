services:
  - type: web
    name: people-counter
    env: python
    region: oregon
    plan: free
    buildCommand: pip install -r requirements_render.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
